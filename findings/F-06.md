# F-06  The exhaust premium — platform ARPU vs. the price of your raw data

CLAIM (one sentence, specific, falsifiable) [PARTIAL — denominator pending]:
  The big ad platforms earn tens of dollars in revenue per user per year, versus the
  ~$0.36 a person's raw data reportedly sells for — an "exhaust premium" of ~100x or more.
  Numerator (revenue) is MEASURED; per-user denominator and the $0.36 price are still to
  be sourced (see NEEDS-DATA).

TYPE:        MEASURED (revenue) + REPORTED (user counts / ARPU, pending) + REPORTED
             (the ~$0.36 price, needs a primary)
NUMBERS:     FY2025 total revenue (SEC XBRL, calendar-year frames):
             - Alphabet .. $402.8B (YoY +15.1%)
             - Meta ...... $201.0B (YoY +22.2%)
             - Snap ......   $5.9B (YoY +10.6%)
             - Pinterest .   $4.2B (YoY +15.8%)
             - Reddit ....   $2.2B (YoY +69.4%)
             ARPU = revenue / average users: **NEEDS-DATA** (denominators below).
             "~$0.36 per person" raw-data price: **NEEDS-DATA** (no primary yet).
SOURCES:     SRC-006 SEC EDGAR companyfacts XBRL (Tier A) for revenue. User metrics will
             be REPORTED from each company's 10-K (not XBRL-tagged).
METHOD:      src/acquire/sec_edgar.py (ticker->CIK via company_tickers.json, then
             companyfacts) + src/analyze/sec_revenue.py (merge us-gaap:Revenues with
             RevenueFromContractWithCustomerExcludingAssessedTax across the 2018 ASC-606
             tag switch; take the latest CY#### annual frame). results/sec_revenue.json.
COVERAGE:    FY2025 full-year revenue, 5 platforms.
CONFIDENCE:  High for revenue (verified: the merge fix moved META/SNAP off a stale 2017
             frame to current FY2025; YoY values match known trends). The premium itself
             is pending the denominator.
NOVELTY:     Revenue is public; the ARPU-vs-$0.36 CONTRAST is the original cut. NOTE:
             Alphabet/Google reports no per-user metric -> excluded from ARPU (revenue
             only). Snap/Reddit/Pinterest/Meta each report a user metric + ARPU in filings.
ALTERNATIVES / confounders:
             - ARPU period/geography: companies report global vs. regional ARPU and on
               an average-user base; match the user period to the revenue period.
             - ad revenue vs. total revenue (use total for the headline, note the split).
             - the ~$0.36 figure is widely cited but needs a traceable primary before use.
ROBUSTNESS:  Merged-concept fix verified; YoY sanity checks pass; revenue cross-checks vs
             prior-year known totals.
NEEDS-DATA:  (1) Per-user metric per company from the latest 10-K (Meta family DAP; Snap
             DAU; Reddit DAUq; Pinterest global MAU) -> compute ARPU. (2) A primary source
             for the ~$0.36 raw-data price (or replace with a sourced broker price).
VISUAL:      ARPU bars per platform vs. a $0.36 reference line; the multiple annotated.
CHAPTER:     3 (the exhaust economy).
STATUS:      DRAFT (NEEDS-DATA: per-user denominators + the $0.36 primary)
