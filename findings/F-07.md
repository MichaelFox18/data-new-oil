# F-07  The data crossover — frontier training size meets the human-text stock ~2029

CLAIM (one sentence, specific, falsifiable):
  Extrapolating the measured frontier trend of LLM training-set size (record-setting
  language models growing ~2.2x/year), frontier training reaches Epoch's reported
  effective stock of human public text (~4e14 tokens) around 2029 (modeled 2027-2031) —
  independently landing inside Epoch's own reported 2026-2032 "data wall" window — and the
  latest frontier model (Qwen3-Max, 36 trillion tokens) is already only ~1 order of
  magnitude below the total usable stock.

TYPE:        MEASURED (frontier trend) + REPORTED (Epoch stock + window) + MODELED (the
             extrapolated crossover year).
NUMBERS:     Frontier trend (record-setting language models, 2018+): 2.19x/yr (1.13
             doublings/yr; n=13 frontier models). Latest frontier: Qwen3-Max = 3.6e13
             tokens (1.05 orders of magnitude below the 4e14 stock). Stock (REPORTED,
             Epoch): ~4e14 effective tokens; 95% CI 1.3e14-2.1e15. MODELED crossover at
             the effective stock: ~2028.7 (CI band 2027.3-2030.9). Epoch's REPORTED
             exhaustion window: 2026-2032.
SOURCES:     SRC-001 Epoch model dataset (Tier A) for the frontier trend; SRC-008 Epoch
             stock estimate (Tier A, REPORTED — arXiv:2211.04325v2). Triangulated: our
             independent crossover lands inside Epoch's published window.
METHOD:      src/analyze/crossover.py — filter Domain=Language; running-max frontier;
             log-linear fit on 2018+ frontier points; solve trend == stock for the
             crossover year + CI bounds. results/crossover.json. Figure:
             src/viz/crossover.py -> figures/crossover.png (all labels from results/).
COVERAGE:    Notable language models with tokens + date; the frontier (record-setting)
             subset drives the trend.
CONFIDENCE:  Medium-High. The independent crossover landing inside Epoch's 2026-2032
             window is a strong triangulation; but the crossover YEAR is MODELED
             (extrapolation), sensitive to the frontier-fit window and the stock value.
NOVELTY:     The crossover is Epoch's published thesis; the value here is rebuilding it
             from first-party data (not a screenshot) AND independently reproducing the
             timing. Hero visual / rigorously rebuilt, not a new discovery.
ALTERNATIVES / confounders:
             - Frontier-fit sensitivity: which models count as "frontier" and the start
               year move the slope; report a sensitivity band.
             - Stock definition: effective/quality-adjusted (~4e14) vs raw indexed web
               (~510T) vs multimodal — different walls.
             - Overtraining (Chinchilla-suboptimal) pulls the wall EARLIER; synthetic data
               pushes it LATER (the deferral overlay, Q5 — still to model).
ROBUSTNESS:  Independent crossover (~2029) falls inside Epoch's reported 2026-2032 window;
             latest frontier (36T) is ~1 OM below stock (sanity); deterministic re-run.
             Planned: sensitivity to fit window + frontier definition; the synthetic-data
             deferral overlay (Q5).
VISUAL:      figures/crossover.png — frontier trend + projection vs the shaded stock band
             and the 2026-2032 window, crossover marked. Fed by results/crossover.json.
CHAPTER:     5 (the strange wall) -> 7 (does the streak break?).
STATUS:      DRAFT (GATE 6; crossover year is MODELED and labeled as such)
