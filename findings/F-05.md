# F-05  Texas free text gives NO second-state AI signal (negative result)

CLAIM (one sentence, specific, falsifiable):
  Texas brokers' free-text registry fields do not provide a usable AI signal: 0 of 397
  registered brokers mention "artificial intelligence", "machine learning", "generative",
  "LLM", or "model training", and the 9 apparent matches are all standalone-"ai" false
  positives (e.g. ".ai" company names like Nyne.ai).

TYPE:        MEASURED (negative result)
NUMBERS:     397 TX brokers. Substantive AI/ML term mentions: artificial intelligence 0,
             machine learning 0, generative/genai 0, LLM/language model 0, model training
             0, neural/deep learning 0. Standalone "ai" regex hits: 9 — all false positives
             (Nyne.ai Corp, Intent IQ, Innovation Brands, Explorium, Faraday, Dun &
             Bradstreet, ...), matching ".ai" names, not AI-use descriptions.
SOURCES:     SRC-005 Texas registry (Tier A), free-text "Categories of Data Processed and
             Transferred" + "Additional Information or Explanation".
METHOD:      src/analyze/tx_ai_mentions.py — regex term scan over the joined free text;
             per-term counts retained. Output: results/tx_ai_mentions.json.
COVERAGE:    397 TX brokers, the two free-text fields. CY2026 export.
CONFIDENCE:  High — a clean zero on every substantive AI term; the only hits are an
             obviously spurious token.
NOVELTY:     n/a (negative).
ALTERNATIVES: Brokers may well USE AI but simply don't describe it in these fields; the
             Texas form doesn't ASK about AI (unlike California's structured "sold to a
             GenAI developer" flag). So absence here means the field is not a measurement
             instrument for AI use — not that TX brokers don't use AI.
ROBUSTNESS:  Per-term breakdown + examples confirm the 9 hits are ".ai" false positives.
IMPLICATION: California's structured GenAI-sharing flag (F-01) remains the ONLY usable
             broker-side AI signal across the four registries. Do not re-chase TX/OR/VT
             free text for it; for more broker-AI signal, go to the AI money trail (Path 4).
STATUS:      REJECTED (negative result — kept to prevent re-chasing, per §8)
